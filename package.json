{
  "name": "heirlock",
  "private": true,
  "version": "0.1.0",
  "description": "HEIRLOCK — AI Finance OS on SoSoValue / SoDEX / SSI",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev:api": "pnpm --filter @heirlock/api dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:ai": "pnpm --filter @heirlock/ai-provider test:e2e",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20"
  }
}
